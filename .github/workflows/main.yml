name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
            pwd
            echo "Hello World!"
            
      - name: Load .env file
        uses: xom9ikk/dotenv@v2
        with:
            path: ./environments/env
            #mode: development
            #load-mode: strict

      - name: Some other action
        run: |
            echo "Variable 1: ${{ env.FIXTURES_4 }}"
            echo "Variable 2: ${{ env.FIXTURES_5 }}"
            echo "Variable 3: ${{ env.FIXTURES_6 }}"
  
            # test action works running from the graph

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          path: ./environments/case.env
          
      # env.PROTOCOL == 'https'
      # env.HOST == 'example.com'
      # env.PORT == '8080'
      # env.URI == 'https://example.com:8080'